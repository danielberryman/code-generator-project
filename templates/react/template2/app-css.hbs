.App { display: flex; } /* MAIN */ .main-container { width: calc(100% - 55px); }
.banner-container { background-color: lightgrey; height: 6rem; width: 100%; }
.page-container { padding: 2rem; } @media (min-width: 960px) { /* NAV */
.nav-container { width: 20rem; display: flex; flex-direction: column; }
.nav-logo { margin: 0 1rem; } .nav-titles { display: flex; flex-direction:
column; justify-content: center; margin-right: 1rem; } .nav-title,
.nav-sub-title { color: white; display: block; margin: 0; width: 100%; }
.nav-title { font-size: 1.25rem; } .nav-sub-title { font-size: .75rem;
line-height: 1rem; } }