function techCallback(tech) {
  // Pass tech back to the prompt loop because next we just get the 1st prompt for the specific technology
}

function setupCallback() {
  // run the instructions for setup
}
